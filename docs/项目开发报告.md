# 🏭 工业巡检可视化大屏与AI智能分析系统 - 项目开发报告

**项目名称**: Leida Industrial Dashboard & AI Sentinel
**开发周期**: 2026年1月
**核心技术**: Three.js, ECharts, Generative AI (ERNIE), Python Middleware, WebSocket/HTTP

---

## 🏗 一、 项目架构与工程概览

本项目旨在构建一个**现代化、智能化的工业数字孪生管理平台**。系统集成了 3D 可视化、实时数据流处理以及基于大语言模型（LLM）的智能决策辅助功能。

### 1.1 系统拓扑设计
*   **前端交互层**: 采用 HTML5 + ES6 构建响应式大屏界面，实现“控制台(`dashboard_system.html`)”与“3D视口(`dashboard.html`)”的双层架构。
*   **3D 渲染引擎**: 基于 **Three.js** 搭建锅炉房数字孪生场景，实现与物理世界的 1:1 坐标映射。
*   **AI 智能层**: 集成 **百度文心一言 (ERNIE-X)** 大模型，通过自定义 Python 中间件实现数据分析。
*   **数据通讯层**: 设计了 `Mock Data` -> `Importer` -> `Dashboard` -> `3D Engine` 的完整数据流转链路，并采用 `window.postMessage` 实现了跨 Frame 的毫秒级数据同步。

---

## 💻 二、 核心功能模块与工作量详述

### 🚀 2.1 生成式 AI 智能分析助手 (AI Agent Integration)
*这是本项目的核心创新点，实现了从“看数据”到“问数据”的跨越。*

*   **RAG (检索增强生成) 初步实现**:
    *   开发了上下文构建引擎，系统会自动提取最近的 **50条关键巡检记录**（包含时间、温湿度、压力、异常状态），动态组装成 LLM 的 Prompt Context。
    *   使 AI 具备了“现场感知能力”，能够针对当前的具体设备状态生成诊断报告，而非泛泛而谈。
*   **Python 中间件服务开发 (`server.py`)**:
    *   为解决浏览器端的 CORS（跨域资源共享）安全限制，自主开发了基于 Python `http.server` 的轻量级代理服务器。
    *   实现了请求转发、API 鉴权管理、以及错误熔断机制，确保 AI 服务的稳定性。
*   **智能交互 UI**:
    *   开发了流式对话界面，支持 Markdown 渲染（用于生成结构化报告）。
    *   集成了快捷指令系统：“生成今日巡检简报”、“分析所有异常告警”，一键触发复杂任务。

### 🌍 2.2 3D 数字孪生可视化 (3D Visualization)
*   **场景重构与优化**:
    *   解决了模型导入后的尺度失真问题，重写了坐标映射算法，将 AR (增强现实) 采集的米级坐标精确映射到 3D 场景中。
    *   实现了相机的其智能跟随算法，解决视角过远或遮挡问题。
*   **深度交互**:
    *   为 3D 场景添加了射线检测 (Raycaster)，实现了点击设备即可弹出详细状态卡片的功能。

### 📊 2.3 工业级 UI 重构与数据可视
*   **视觉升级**:
    *   执行了全系统的 UI 重构，将原有的测试版配色替换为专业的 **Industrial Blue (#3b82f6)** 视觉体系。建立了一套统一的 CSS 变量库。
*   **ECharts 动态图表**:
    *   开发了“温压趋势图”和“问题分布饼图”，并实现了图表与数据源的实时绑定。数据更新时，图表会自动呈现过渡动画。

### 🔄 2.4 数据管理系统
*   **健壮的数据清洗逻辑**:
    *   实现了并容错性高的数据导入器，能够自动识别并兼容 `temp` vs `temperature` 等异构字段。
    *   开发了“追加模式”与“覆盖模式”的数据加载策略，解决了旧数据丢失的问题。
*   **测试数据模拟**:
    *   编写了 `mock_data` 生成脚本，模拟了单点报告、多点并发报告等多种工业现场数据场景。

---

## 🛠 三、 技术难点攻克

1.  **AI 上下文超限问题**: 通过切片算法 (`slice(0, 50)`) 和字段精简策略，在保证 Token 不溢出的前提下，最大化了 AI 的信息获取量。
2.  **CORS 跨域阻断**: 浏览器的安全策略阻止了前端直接调用大模型 API。通过手写 Python 代理层，成功并在本地搭建了安全的转发通道。
3.  **多源数据同步**: 在主控界面、3D 视图、图表组件三个独立的模块之间，设计了基于事件总线的数据分发机制，确保所有组件状态的一致性。

---

## 📝 四、 总结

本项目不仅仅是一个展示大屏，更是一个集成了 **AR 数据采集 (iOS端对接)**、**3D 复现**、**大数据统计** 以及 **AI 辅助决策** 的综合性工业软件原型。它展示了如何利用最新的 AI 技术来赋能传统工业巡检场景，极大地提高了数据分析效率和异常响应速度。
