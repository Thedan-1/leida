# 🏭 工业安全巡检大屏系统 (Leida Dashboard)

这是一个基于 Web 和 AI 的工业巡检可视化大屏系统。

## 📂 包含文件

*   **dashboard_system.html**: 系统主入口文件 (大屏界面)。
*   **dashboard/**: 3D 场景与前端依赖资源。
*   **mock_data/**: 演示用的模拟 JSON 数据。
*   **server.py**: 本地 Python 代理服务器 (用于支持 AI 跨域请求)。
*   **test_ai.py**: AI 连接测试脚本。

## 🚀 启动方式

由于系统包含 **AI 智能助手** 功能，需要通过本地服务器运行以解决浏览器跨域 (CORS) 问题。

### 第一步：启动服务器
请在终端 (Terminal) 或命令提示符 (CMD) 中，进入当前文件夹，并运行：

```bash
python3 server.py
```

*如果您的电脑没有安装 Python 3，请先从官网下载安装。*

### 第二步：访问系统
服务器启动后，打开浏览器 (推荐 Chrome/Edge)，访问以下地址：

👉 **http://localhost:8000/dashboard_system.html**

## 💡 功能提示

1.  **AI 智能助手**: 点击左侧边栏的 "🤖 AI 智能助手"，可以与数据进行对话，或生成巡检简报。
2.  **数据导入**: 在 "数据管理" 页面，你可以导入 `mock_data` 文件夹中的 `.json` 文件来模拟巡检数据。
3.  **3D 视图**: 系统会自动加载 3D 锅炉房场景，并根据数据映射报警点位。

---
Generated by Leida Dev Team
